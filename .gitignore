node_modules
dist
dist-ssr
.DS_Store
*.local
.env
.env.local
.env.*.local

# vite
*.log
.vite

# tsc build info
*.tsbuildinfo

# fetched GitHub data (generated by CI; stored under apps/web/public/data after monorepo restructure)
apps/web/public/data/**/*.json
!apps/web/public/data/.gitkeep

# api build/runtime artifacts
apps/api/dist/

# prisma — keep migrations, ignore local cache
apps/api/.prisma/

# editor
.vscode/*
!.vscode/extensions.json
.idea

# local tool state (not repo-wide)
.serena/
.claude/settings.local.json
.playwright-cli/
.playwright-cli-config.json
